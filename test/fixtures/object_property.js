const objectWithFunction = {
  /**
   *  Public: object function
   *
   *  * `node` {Object} Spidermonkey AST node
   *  * `arr` {Array} of {Objects}
   *  * `returnsArr` {Array} of Returns
   */
  objectFunction(value) {
    return value;
  }
}
